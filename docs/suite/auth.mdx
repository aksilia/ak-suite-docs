# Autenticazione

Il modulo di Autenticazione è uno dei componente essenziali di Suite, progettato per fornire un'interfaccia sicura e intuitiva per l'autenticazione degli utenti. Questo modulo consente la verifica dell'identità degli utenti attraverso metodi come **email e password**, o tramite OAuth2 per mezzo dei provider **Google** e **Microsoft**, assicurando un accesso sicuro e protetto alle risorse del sistema.

<Warning>Suite permette solo il login all'utente. Per la registrazione è necessario rivolgersi al proprio consulente o al supporto tecnico.</Warning>

## Accesso

Per accedere al proprio account, l'utente deve selezionare la rispettiva modalità di accesso, scegliendo tra Google o Microsoft o inserendo la propria email e password. Cliccando su "Continua" l'accesso al sistema viene verificato.
 
Link di autenticazione: [sign in](https://app.aksiliasuite.com/auth/signin)

<Image src="/assets/ak_login.png" alt="ak login" height="500" />

E' possibile effettuare il login in tre differenti modalità:

- **Email**: inserendo l'email e password
- **Google**: accedendo con le credenziali di un account Google aziendale
- **Microsoft**: accedendo con le credenziali di un account Microsoft aziendale

<Image src="/assets/ak_login_google.png" alt="ak login" height="500" />

## Recupero password

Se l'utente ha dicmenticato la password, può recuperarla inserendo il proprio indirizzo email. Alla richiesta di reset, l'utente riceverà una email con un link per reimpostare la password.

Link di reset: [password reset](https://app.aksiliasuite.com/auth/reset)

<Image src="/assets/ak_reset_password.png" alt="ak login" height="500" />

La pagina di reset richiede di inserire e confermare la nuova password. Abilitando l'emoji 🫣 si può visualizzare la password inserita al fine di verificare i requisiti minimi.

I requisiti di sicurezza per le password sono i seguenti:

- 16 caratteri
- 1 lettera maiuscola
- 1 lettera minuscola
- 1 numero
- 1 carattere speciale

<Image src="/assets/ak_reset.png" alt="ak reset password" height="500" />
